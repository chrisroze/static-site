# This script runs the main application. It assumes that you have Python 3 installed and that the necessary dependencies are available. 
# Make sure to activate your virtual environment if you are using one before running this script.     
python3 src/main.py